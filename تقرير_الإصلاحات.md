# تقرير الإصلاحات — Calibration Certificates_v11

تاريخ التقرير: 2026-08-04

## ملخص

نُفّذت جميع الإصلاحات المتفق عليها في نطاق العمل: الأخطاء الوظيفية (1–14)، مشاكل الجودة الاسمية، وإزالة الكود الميت. أُضيفت 11 اختباراً جديداً (من 9 إلى 20 اختباراً) تغطي إصلاحات البنية التحتية، وكلها ناجحة ومحدّدة السلوك. جميع المشاريع تبني بنجاح دون أخطاء، مع بقاء تحذيرَي الاسمية الأساسيين فقط في مشروع WPF.

---

## خط الأساس (قبل الإصلاحات)

- `BlueMax.Infrastructure`: يبني بدون أخطاء وبدون تحذيرات.
- `BlueMax.Presentation.Wpf`: يبني بتحذيرين فقط (CS8603 في `StringToBrushConverter.cs:17` و`BooleanToTextDecorationsConverter.cs:17`).
- `BlueMax.KeyGen.UI`: يبني بدون أخطاء وبدون تحذيرات.
- الاختبارات: 9/9 ناجحة.

---

## الأخطاء الوظيفية

### الخطأ 1 — اختلاط الحقل Brand مع DeviceType في المستند
- **قبل:** كان المستند يكتب `DeviceType` في موضع العلامة التجارية، وقاموس البيانات يستخدم مفتاحاً خاطئاً.
- **بعد:** `CertificateDocumentService.cs` يستخدم `Brand: {workOrder.Brand}` ومفاتيح القاموس `["Brand"]`/`["brand"]` (استبدال شامل).
- **الملفات:** `BlueMax.Infrastructure\CertificateDocumentService.cs`.

### الخطأ 2 — قراءة الإعدادات الافتراضية للمعايرة من ملف خاطئ
- **قبل:** `ApplyCalibrationTemplate` يقرأ ملف `CalibrationDefaults.json` العام لجميع الأجهزة.
- **بعد:** أُضيف `CalibrationDefaultsStore.LoadRows(deviceType)` الذي يقرأ `CalibrationDefaults_{deviceType}.json` (نفس صيغة الحفظ)، مع تحديث المستدعي في `CertificatesViewModel.ApplyCalibrationTemplate` لاستخدامه مع فلترة الصفوف الفارغة وبناء قاموس آمن.
- **الملفات:** `BlueMax.Infrastructure\CalibrationDefaults.cs`، `BlueMax.Presentation.Wpf\ViewModels\CertificatesViewModel.cs`.

### الخطأان 3 و4 — قراءة القوالب ومطابقة الهوية
- **قبل:** `Load(deviceType, templateName)` يقرأ ملف `ReportTemplates.json` العام بينما الحفظ يكتب في مجلد نوع الجهاز (عدم تطابق)، وكان `FindTemplateId` يبني معرّفاً من خصائص غير مستقرة.
- **بعد:** `Load(deviceType, templateName)` يعيد `ReportTemplateXml?` بقراءة `Templates\{deviceType}\*.json` مع مطابقة اسم غير حساسة لحالة الأحرف؛ و`FindTemplateId` يعيد `StableHash("deviceType|templateName")` المستقر عبر SHA256 (أول 4 بايتات مع `int.MaxValue`) ويعيد `null` عند غياب القالب.
- **الملفات:** `BlueMax.Infrastructure\ReportTemplateStore.cs`.

### الخطأ 5 — إرجاع متغير خاطئ في توليد الشهادة
- **قبل:** الدالة تعيد `pdfPath` رغم أن المسار المُنشأ هو `docxPath`.
- **بعد:** الإرجاع الموحّد `return docxPath` في جميع المسارات.
- **الملفات:** `BlueMax.Infrastructure\CertificateDocumentService.cs`.

### الخطأ 6 — معاينة PDF المكسورة
- **قبل:** تحويل PDF→PNG عبر مكتبة داخليّة معطوبة.
- **بعد:** إعادة كتابة `PdfConverter.TryConvertPdfToPng` لاستخدام LibreOffice: `soffice --headless --norestore --convert-to png` مع مهلة 120 ثانية؛ وحلّ مسار soffice من إعداد `LibreOfficeProgramPath` ثم مواقع ProgramFiles القياسية ثم `soffice` الافتراضي. بقيت توقيعات الدوال كما هي فبقي المستدعون متوافقين.
- **الملفات:** `BlueMax.Infrastructure\PdfConverter.cs`.

### الخطأ 7 — معاينة إيصال الإيجار
- **قبل:** نافذة معاينة تستخدم PDF خارجي.
- **بعد:** إعادة بناء `RentalReceiptWindow` كمستند `FixedDocument` A4 بالعربية (النمط نفسه في `MaintenanceReceiptWindow`): ترويسة الشركة، رقم/تاريخ الإيصال، جداول العميل والجهاز والإيجار مع صف الحالة، والملاحظات.
- **ملاحظة:** النافذة orphaned حالياً (لا يفتحها أي أمر؛ أمر الطباعة يولّد DOCX مباشرة) — أُصلحت مع ذلك لإزالة الاستدعاء الخارجي للـ PDF.
- **الملفات:** `BlueMax.Presentation.Wpf\Views\RentalReceiptWindow.xaml.cs`، `ViewModels\RentalsViewModel.cs`.

### الخطأ 8 — فلترة اسم القالب غير الحساسة
- **بعد:** `Load(templateName)` في `ReportTemplateStore` يفلتر بأسماء مطابقة مع تجاهل حالة الأحرف، وكذلك قائمة الملفات في `ListTemplates`.

### الخطان 9 و10 — التبديل الصامت بين SQL Server وSQLite
- **قبل:** عند تعذّر الوصول إلى SQL Server المكوّن كان الكود يتحول صامتاً إلى SQLite — مما قد يخفي بيانات على المستخدم.
- **بعد:** في `AppDbContext`، إذا وُجدت سلسلة اتصال SQL وفشل فحص الوصول يُرمى `InvalidOperationException` برسالة عربية واضحة: «تم تكوين SQL Server لكن لا يمكن الوصول إلى الخادم...». يبقى SQLite حصراً عند غياب سلسلة SQL من الأساس. كما ضُبط `ConnectTimeout = 3` في فحص الوصول.
- **للاختبارية:** استُخرج منطق القرار إلى `AppDbContext.ConfigureProvider(optionsBuilder, sqlConnectionString)` (internal) للاختبار الحتمي دون لمس حالة التطبيق الحقيقية.
- **الملفات:** `BlueMax.Infrastructure\AppDbContext.cs`.

### الخطأ 11 — تقرير المخزون المطبوع (بترقيم الصفحات فقط)
- **قبل:** صفحة A4 واحدة لكل القطع؛ المخزون الكبير يُقتطع صامتاً.
- **بعد:** `InventoryViewModel.PrintReport` يوزّع الصفوف على صفحات فعلية (تقدير ارتفاع الصف)، مع تكرار ترويسة الجدول في الصفحات التالية، وترقيم «الصفحة X من Y» عند تعدد الصفحات. أُبقيت العناوين العربية الثابتة كما هي (نمط مستندات الطباعة في التطبيق كافة).
- **الملفات:** `BlueMax.Presentation.Wpf\ViewModels\InventoryViewModel.cs`.

### الخطأ 12 — استعلامات إحصائيات الصفحة الرئيسية
- **النتيجة:** التحقق من الكود الفعلي أثبت أن `HomeViewModel.LoadStatistics` يستخدم أصلاً استعلامات `.Count()` صحيحة تُترجم إلى `COUNT(*)` — لا يحتاج تعديلاً.
- **الملفات:** `BlueMax.Presentation.Wpf\ViewModels\HomeViewModel.cs` (بلا تغيير).

### الخطأ 13 — قيمة الحالة المرمّزة في إيصال الإيجار
- **قبل:** `"Active"` مكتوبة حرفياً في معاينة الإيصال.
- **بعد:** أُضيفت خاصية `RentalReceiptData.Status` وتستخدمها النافذة بدل النص الثابت.
- **الملفات:** `BlueMax.Presentation.Wpf\ViewModels\RentalsViewModel.cs`، `Views\RentalReceiptWindow.xaml.cs`.

### الخطأ 14 — خط غير موجود «Cairo 2026»
- **قبل:** استشهادات بخط `Cairo 2026` في XAML.
- **بعد:** استبدال شامل بـ `Cairo` في 6 ملفات XAML (`MainWindow`, `Styles`, `ClientsView`, `HelpWindow`, `MaintenanceView`, `RentalsView`)، والتحقق من عدم بقاء أي استشهاد.

---

## مشاكل الجودة الاسمية (المتفق عليها حصراً)

| البند | الإجراء |
|---|---|
| `AsyncRelayCommand.Execute` | `async void` ملتصق بواجهة ICommand؛ أُضيف `catch (Exception)` قبل `finally` لمنع الاستثناءات غير المرصودة. |
| `PreviewTemplate` | تحويلها من `async void` إلى `async Task PreviewTemplateAsync()` مع تسجيلها عبر `AsyncRelayCommand`. |
| مواضع `.Result` | `RefreshTemplateOptions` (سطر 1836) و`ApplyCalibrationTemplate` (سطر 2662): تحويل استمرارية `ContinueWith` إلى غير متزامنة مع `await`. موضع 2715 إيجابية كاذبة — فهو خاصية `CalibrationRow.Result` وليس `Task.Result`. |
| `WordTemplateEngine` | `ToList()` قبل `row.Remove()` أثناء تعداد الصفوف؛ وكشف نوع الصورة عبر `DetectImagePartType` (يعيد `PartTypeInfo`) بدل `ImagePartType.Jpeg` الثابت — اكتُشف عبر خطأ CS0722 أن `ImagePartType` في OpenXml 3.4.1 كلاس ثابتة. |
| `LicenseStore` | حذف `LoadTrialStartDate`/`SaveTrialStartDate` غير المستخدمة؛ `SaveSettings` أصبحت ذرّية (ملف مؤقت ثم `File.Move` مع `overwrite`). |
| `BackupService` | أصبح `IDisposable` مع `Dispose()` للعداد؛ أمر النسخ يستخدم `GetDatabaseName(connectionString)` بدل `MyAppDb` المرمّز. |
| `CloudApiBase`/`CloudApiKey` | تحويلهما من ثوابت إلى خاصيتين static تقرآن `AppSettings["CloudApiBase"]`/`["CloudApiKey"]` مع الاحتفاظ بالقيم الأصلية كاحتياط. |
| تكرار `SaveAppSetting` | كانت 6 نسخ متطابقة من طريقة حفظ إعداد واحد في 6 ملفات؛ أُنشئ `Services\AppSettingHelper.cs` موحّد وأُعيد توجيه كل النسخ إليه (بما فيها `SaveVerification` المشابهة). |

---

## الكود الميت (حُذف)

بعد التحقق من عدم الاستخدام (grep) حُذفت الملفات:
- `BlueMax.Infrastructure\CertificateTokenService.cs`
- `BlueMax.Infrastructure\AesKeyStore.cs`
- `BlueMax.Infrastructure\DatabaseSecurity.cs`
- `BlueMax.Infrastructure\SqliteSchemaHelper.cs`
- `BlueMax.Infrastructure\ZplImageHelper.cs`
- `BlueMax.KeyGen.UI\DeactivateLicense.cs`

**مُبقي:** `SecureConnectionStringStore` (مستخدم في `BackupSettings.cs` و`AppDbContext`)، `StickerImageHelper`، و`CalibrationDefaultsRow` (مستخدم في `CertificatesViewModel`).

---

## الاختبارات

أُضيف `BlueMax.Domain.Tests\InfrastructureFixesTests.cs` (11 اختباراً جديداً) مع إضافة مرجع `BlueMax.Infrastructure` و`InternalsVisibleTo`:

- **ReportTemplateStore (6):** حفظ/قراءة لنوع جهاز محدد؛ مطابقة اسم غير حساسة؛ `null` لقالب مفقود؛ `ListTemplates` يتضمن القالب المحفوظ؛ `FindTemplateId` مستقر وغير حساس؛ `FindTemplateId` يعيد `null` لقالب مفقود.
- **CalibrationDefaultsStore (3):** حفظ/قراءة `LoadRows` لنوع الجهاز؛ عزل الأنواع (كل نوع ملفه المستقل)؛ إرجاع قائمة فارغة عند غياب الملف.
- **AppDbContext (2):** `ConfigureProvider` يرمي `InvalidOperationException` لـ SQL غير قابل للوصول (بدل الرجوع الصامت)؛ ويعيّن SQLite عند غياب سلسلة SQL.

استُخدمت أسماء فريدة (GUID) مع تنظيف تلقائي في `finally` لتجنّب تلويث إعدادات التطبيق الحقيقية. أثناء التطوير تبيّن أن اختباراً مبكراً يعتمد على ملف التخزين الحقيقي كان متقلباً (قفل مؤقت للملف) فاستُبدل باختبار حتمي عبر `ConfigureProvider`.

**النتيجة:** 20/20 ناجحة (تحققت مرتين).

---

## نتائج البناء النهائية

| المشروع | الأخطاء | التحذيرات |
|---|---|---|
| BlueMax.Domain | 0 | 0 |
| BlueMax.Infrastructure | 0 | 0 |
| BlueMax.Presentation.Wpf | 0 | 2 (CS8603 — هما تحذيرا خط الأساس نفسه) |
| BlueMax.KeyGen.UI | 0 | 0 |
| الاختبارات | — | 20/20 ناجحة |

---

## ملاحظات

- `RentalReceiptWindow` نافذة orphaned؛ أُصلحت ضمن النطاق لكن أمر الطباعة الفعلي (`PrintRentalReceipt`) يولّد DOCX مباشرة ولا يفتحها — قرار مستخدم اختيارياً للربط لاحقاً.
- ملفات التشخيص المؤقتة ومشروع التصحيح المؤقت في مجلد temp أُزيلت، وحالة التخزين الحقيقية (`EncryptedConnectionString.bin`) أُعيدت إلى الحالة النظيفة (محذوفة) لأن التطبيق على هذه الآلة يعمل بـ SQLite أصلاً (لا يوجد SQL Server مثبّت).
